{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "flutter rebuild",
            "type": "shell",
            "command": "chmod -R 777 build && flutter clean && flutter run"
        }
    ]
}